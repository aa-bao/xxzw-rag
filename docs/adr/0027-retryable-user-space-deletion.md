# 管理员可永久删除整个用户空间

账户管理员在重新验证自身密码并输入目标用户名确认后，可以发起持久化用户删除任务。系统立即将目标账户标记为 `deleting`、撤销全部 Session，并逐级永久删除其知识库、文件、索引、对话和日志；管理员仍不能读取目标用户内容，失败任务可重试，审计仅保留脱敏的账户删除事件。管理员不能删除自己，也不能删除或禁用最后一个有效账户管理员；管理员角色交接需要再次验证当前管理员密码。
