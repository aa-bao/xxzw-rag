# MVP 使用纯向量检索并保留稳定检索接口

MVP 只实现单知识库范围内的 ChromaDB Top-K 向量检索，不实现自动路由、跨库合并、BM25、融合、Reranker、查询重写或 LangGraph。问答流程只依赖检索模块的 `retrieve()` 接口，ChromaDB 细节隐藏在模块内部；后续质量优化通过替换模块实现或增加内部检索阶段完成，不改变问答层调用方式，也不提前创建尚无第二实现的 Adapter 层。
