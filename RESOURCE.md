# 资源与副作用边界

## 服务端资源

| 组件 | CPU | 内存 | PID | 临时盘 | 持久化 |
| --- | ---: | ---: | ---: | ---: | --- |
| 前端 | 0.5 | 256 MiB | 128 | 32 MiB | 无 |
| 后端 | 1 | 1024 MiB | 256 | 128 MiB | MySQL、上传文件、Chroma、SQLite FTS |

资源上限与 `rpa-application.yaml` 一致。后端固定以非 root 用户运行，不依赖宿主机绝对路径。

## 外部依赖

- MySQL：用户、会话、知识库、文档、任务及模型设置的事实来源。
- OpenAI 兼容模型中转：嵌入与对话请求；网络失败不得形成业务成功响应。
- 本地持久卷：原始上传、Chroma 向量索引和 SQLite FTS 索引。

## 业务副作用

- 上传文档会写入原始文件、数据库、向量索引和 FTS 索引。
- 删除知识库或文档会触发异步清理；失败保留可重试状态，不静默报告成功。
- 对话会保存服务端权威历史，并向模型服务发出请求。
- 模型与用户设置变更仅允许账户管理员执行，并写入项目数据库。

## 密钥与网络边界

- Git 和镜像不得包含数据库密码、模型 API Key、Cookie 或访问令牌。
- 运行期密钥由部署环境注入；日志、健康接口与版本接口不得回显密钥或连接串。
- 内网数据库和模型中转按明确地址访问，不依赖交互用户的隐式系统代理。

## 当前平台接入边界

已实现（仓库内）：平台 SSO（一次性授权码 + PKCE + state，独立 HttpOnly 项目会话，30 分钟/5 分钟
刷新）、ProjectPrincipal 权限字符逐接口校验（TEST/PRODUCTION 只接受平台会话）、租户 + dataScope
数据隔离（RESTRICTED 空集合返回空集）、Controller 密钥目录 `/run/tyt-rpa/secrets/`（含数据库
运行凭据）、Conformance 无数据库模式（13 黑盒场景）与 CI 门禁报告。

需要平台侧执行：TEST/PRODUCTION 发布（Controller Release 状态机）、Registry 推送与 Cosign
签名、真实主系统 SSO 黑盒验收。在这些真实外部验收通过前，发布状态必须保持 TEST/不可生产，
不得把本地账号登录误标记为平台 SSO。
