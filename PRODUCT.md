# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

- 普通用户：在本地多用户环境中管理自己的私有知识库、文档与对话。
- 账户管理员：除使用 AI 功能外，负责平台配置与本地用户账户管理，但不能查看其他用户空间中的私有内容。

## Product Purpose

将现有 RAG 知识库系统演进为可持续扩展的 AI 工作台。RAG 知识库是工作台中的一个一级功能模块；后续 AI 能力以新的一级模块加入，而不继续堆叠在 RAG 导航和实现内部。

现阶段成功标准是：保留现有 RAG 业务能力和访问路径的可用性，同时建立清晰、可扩展的两级导航与模块组织方式。

## Positioning

AI 工作台以独立 AI 功能模块组织能力。每个一级模块拥有自己的二级任务入口；平台级管理能力与具体 AI 功能分离。

## Operating Context

- 登录后默认进入“RAG 知识库 → 知识库”。
- “RAG 知识库”包含“知识库”“对话”“映射模板”三个二级入口。
- “系统管理”是独立一级目录，包含“设置”“用户管理”，仅账户管理员可见。
- 在至少出现两个 AI 功能模块前，不新增内容空泛的工作台概览首页。

## Capabilities and Constraints

- 保留现有知识库管理、文档入库、检索测试、对话、映射模板、平台设置和用户管理能力。
- 现有知识库详情中的“文档”“检索测试”“配置”属于具体知识库内部的局部导航，不提升为工作台二级菜单。
- 用户空间仍是私有数据边界，首版不支持跨用户共享。
- 前端技术栈保持 Vue 3、TypeScript、Vue Router、Pinia、Element Plus 和 Vite。
- 后端仍由 Python RAG 服务拥有 RAG 数据；本次工作台导航改造不改变该数据所有权。

## Brand Commitments

- 产品总称改为“AI 工作台”。
- “RAG 知识库”作为现有能力的一级模块名称。
- 界面继续使用中文。

## Evidence on Hand

- 现有前端实现位于 `web/`，后端实现位于 `rag-service/`。
- 业务术语与现有数据边界记录在 `CONTEXT.md`。
- 当前没有已确认的新品牌资产、工作台首页内容或第二个 AI 功能模块。

## Product Principles

- 平台能力与具体 AI 功能分离。
- 新增 AI 功能通过模块注册接入，避免修改散落的导航判断。
- 保持既有用户路径和数据语义稳定，优先渐进迁移。
- 导航只呈现用户有权限访问的模块与入口。
- 不为空洞的平台感提前制造无实际任务的页面。

## Accessibility & Inclusion

- 两级导航必须支持键盘操作、清晰焦点状态、展开状态语义和当前页面语义。
- 桌面端是当前主要形态；窄屏行为需在实现前明确为折叠侧栏或抽屉导航。
