# RAG 知识库系统

本上下文定义 RAG 知识库系统设计与交付阶段中的核心业务术语，避免将验证版本与正式内网版本混为一谈。

## 产品边界

**本机 RAG 知识库**:
当前 MVP 在一台机器上为多个用户提供文档管理、检索与问答；未来集成能力不属于本期交付边界。
_Avoid_: 最终产品、中台 RAG 模块、完整平台

**用户空间**:
每个已认证用户在本机 RAG 知识库中的私有数据边界，用户只能访问自己拥有的知识库、文档、对话和记录，首版不支持共享。
_Avoid_: 租户、工作区、系统账户

**用户**:
通过本地账户登录并拥有一个私有用户空间的人。
_Avoid_: 租户、成员、客户端

**账户管理员**:
负责创建、禁用、重置或永久删除本地用户账户的人，无权查看其他用户空间中的知识库内容。
_Avoid_: 超级用户、数据管理员、租户管理员

**文档**:
用户上传并归属于一个知识库的原始文件及其可检索表示；原始文件必须保留以支持核对与重新入库。
_Avoid_: Chunk、向量、临时上传文件

**文档任务**:
对一份文档执行入库或永久删除的持久化工作记录，可在失败或进程重启后安全重试。
_Avoid_: 导入任务、上传请求、定时同步任务

**模型中转服务**:
由本机 RAG 系统调用并负责路由到自定义模型的独立接口，RAG 系统不绑定具体模型厂商。
_Avoid_: OpenAI、模型本体、RAG 模型

## 数据边界

**RAG 数据**:
知识库、文档、对话、引用与查询记录等由 RAG 上下文产生和管理的数据，仅由 Python RAG 服务拥有。
_Avoid_: 中台 RAG 表、Java 元数据
